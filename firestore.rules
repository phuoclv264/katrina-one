
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents for development purposes.
    // IMPORTANT: These rules are insecure and should be replaced with
    // proper authentication and authorization rules before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
